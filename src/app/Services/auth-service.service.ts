import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class AuthServiceService {
  /* private isAuth:boolean = false;

  // Assume you have a method to check authentication status
  isAuthenticated() {
    console.log(this.isAuth)
    return this.isAuth;
  }
 */
}
